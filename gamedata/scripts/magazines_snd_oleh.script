-- removes ammo pickup sound during Magazines reload
refund_ammo = magazines.refund_ammo
function magazines.refund_ammo(...)
	if itms_manager.timeout_snd_on_take then
		itms_manager.timeout_snd_on_take(250)
	end
    refund_ammo(...)
end